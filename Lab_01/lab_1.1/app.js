// shoppingCart .js - Legacy implementation
//var cart = [];
//var total = 0;
//function addItem(name, price) {
//  cart.push({ name: name, price: price });
//  total += price;
//  updateDisplay();
//}
//function removeItem(index) {
//  var item = cart[index];
//  total -= item.price;
//  cart.splice(index, 1);
//  updateDisplay();
//}
//function updateDisplay() {
//  var cartDiv = document.getElementById("cart ");
//  cartDiv.innerHTML = "";
//  cart.forEach(function (item, index) {
//    var itemDiv = document.createElement("div ");
//    itemDiv.innerHTML = item.name + " - $" + item.price;
//    var removeBtn = document.createElement("button ");
//    removeBtn.onclick = function () {
//      removeItem(index);
//    };
//    itemDiv.appendChild(removeBtn);
//    cartDiv.appendChild(itemDiv);
//  });
//  document.getElementById("total ").textContent = "$" + total;
//}
//function calculateTax(rate) {
//  return total * rate;
//}
//function checkout() {
//  var tax = calculateTax(0.08);
//  var finalTotal = total + tax;
//  alert("Total : $" + finalTotal);
//  cart = [];
//  total = 0;
//  updateDisplay();
//}

// Task 1

```
1. Определите все шаблоны проектирования (или прото-шаблоны), которые вы наблюдаете в этом коде.
Наблюдаю вэтом коде признаки прото-паттерна, который пытается имитировать структуру MVC (Model-View-Controller). Это можно увидить в отсутствии инкапсуляции данных и логики, жёсткая привязка между данными, слабая связь "Данные-Вид".
2. Для каждого выявленного шаблона укажите, к какой категории он относится (создание, структура или поведение) на основе главы 6 учебника.
Предпологаю что данный код относится к структурным паттернам. Но из-за отсутствия чёткой структуры и инкапсуляции, это скорее прото-паттерн, чем полноценный шаблон проектирования.
3. Объясните, почему вы считаете, что это настоящий шаблон или нет.
Из-за выше перечисленных причине, я считаю что это не полноценный шаблон проектирования. Поскольку паттерн должен быть переиспользуемым и инкапсулированным, а данный код не соответствует этим критериям.
```

// Task 2

```
	1. Соответствие цели (7 баллов): Успешно ли это решение решает конкретную проблему? Какую проблему оно решает? Приведите доказательства из кода.
	Данный код успешно решает поставленную задачу, а именно "управление корзиной покупок". В доказательство можно привести функцию updateDisplay(), котороая вызывается после каждого изменения данных (addItem, removeItem), обеспечивая актуальность UI. Код выполняет свою прямую задачу
	2. Полезность (7 баллов): Дает ли это решение доказанную выгоду? Какие преимущества оно предлагает? Есть ли ограничения? 
	Код даёт кратковременную выгоду в виде простого управления корзиной покупок. Код невозможно тестировать изолированноБ так как он напрямую привязан к DOM-элементам 'cart' и 'total'.
	3. Применимость (6 баллов): Применимо ли это решение в широком спектре контекстов или оно специфично для данного сценария? Объясните свое мнение.
	В широком контексте данный код не применим, мы не можем использовать его для разных корзин на одной странице, так как глобальные переменные и жёстко пописанные ID элементов вызовут конфликты
```